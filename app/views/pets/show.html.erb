<h1><%= @pet.name %></h1>

<ul>
  <li>Address: <strong><%= @pet.address %></strong></li>
  <li>Species: <strong><%= @pet.species %></strong></li>
  <li>Found on: <strong><%= @pet.found_on %></strong></li>
</ul>

<%= link_to "Edit #{@pet.name}", pet_edit_path(@pet) %>
<%= link_to "Delete #{@pet.name}", pet_path(@pet), method: :delete, data: { confirm: "Are you to remove #{@pet.name} from database" } %>
<hr>
<%= link_to "Go back to pet list", pets_path %>
